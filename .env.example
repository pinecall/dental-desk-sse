PINECALL_API_KEY=pk_...

# A number from your Pinecall inventory. Optional — without it, browser only.
PHONE=+14847598998

# Optional: write every wire event the SDK receives to this file (debugging).
# PINECALL_LOG=./pinecall.log
